# Countdown Puzzle Solver — Knowledge Distillation

---

## 📌 What Is This

[Countdown](https://huggingface.co/datasets/HuggingFaceTB/Countdown-Task-GOLD) is a combinatorial math puzzle: given a target number and a set of integers, find an arithmetic expression using `+`, `−`, `×`, `÷` that equals the target (each number used exactly once).

This project explores how much reasoning ability can be distilled from a large model (8B) into a small one (1B) using chain-of-thought traces generated by the teacher.

---

## 📊 Results

| Stage | Method | Accuracy |
|---|---|---|
| Baseline (no fine-tuning) | Gemma-3-1B greedy | ~5% |
| After distillation | Gemma-3-1B greedy | **43%** |
| After distillation | Gemma-3-1B Top-5 sampling | **86%** |

**Key insight:** A 1B model distilled from an 8B teacher nearly doubles its accuracy when allowed to sample 5 candidates at inference — no extra training needed, just a smarter decoding strategy.

---

## 🔄 Pipeline Overview

```
Qwen3-8B (teacher)
    │  vLLM batch inference
    │  correct expressions as labels
    ▼
balanced_cleaned_full.csv  ←── also: countdown_5_6_numbers.csv
    │
    │  SFT with LoRA (rank=32)
    │  Unsloth + TRL
    ▼
Gemma-3-1B-IT  (gemma_math_lora_final)
    │
    ├── eval greedy  → 43%
    └── eval Top-5   → 86%
```

---

## 🛠️ Tech Stack

| Component | Tool |
|---|---|
| Teacher inference | `vLLM` — batched generation, float16 |
| Teacher model | `Qwen/Qwen3-8B` — generates correct expressions |
| Student model | `google/gemma-3-1b-it` |
| Fine-tuning framework | `Unsloth` + `TRL SFTTrainer` |
| LoRA | rank=32, alpha=64, targets: `q/k/v/o_proj`, `gate/up/down_proj` |
| Quantization | 4-bit (`load_in_4bit=True`), `adamw_8bit` optimizer |
| Mixed precision | bf16 / fp16 depending on GPU |
| LR schedule | Cosine with warmup |
| Dataset | HuggingFace `Countdown-Task-GOLD` + custom `countdown_5_6_numbers.csv` |
| Hardware | Single GPU, CUDA 12.8 |

---

## 🗂️ Repository Structure

```
countdown-distillation/
│
├── 1_generate_data.ipynb        # Qwen3-8B teacher inference via vLLM
│                                # → qwen3_full_results.csv
│
├── 2_train_gemma.ipynb          # SFT fine-tuning Gemma-3-1B with LoRA
│                                # loads: balanced_cleaned_full.csv
│                                # saves: gemma_math_lora_final/
│
├── 3_evaluate.ipynb             # Greedy vs Top-5 accuracy + breakdown by num count
│  
│
└── README.md
```

---

## 🚀 Quickstart

### 1. Install dependencies

```bash
pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
pip install trl transformers datasets accelerate bitsandbytes peft vllm
pip install "numpy<2"
```

### 2. Generate teacher data (optional — skip if using provided CSV)

```bash
jupyter notebook 1_generate_data.ipynb
```

Loads `Qwen/Qwen3-8B` via vLLM to generate correct arithmetic expressions for each puzzle. Results are saved to CSV incrementally every 100 rows so no progress is lost on long runs.

### 3. Fine-tune Gemma-3-1B

```bash
jupyter notebook 2_train_gemma.ipynb
```

Trains with LoRA (rank=32) using `SFTTrainer`. Uses Gemma's native chat template (`<start_of_turn>user` / `<start_of_turn>model`). Saves LoRA adapter to `gemma_math_lora_final/`.

Key training config:
- 3 epochs, batch 2 + grad accumulation 4 → effective batch 8
- lr = 2e-4, cosine schedule, sequence packing enabled
- ~2% of parameters are trainable (LoRA)

### 4. Evaluate

```bash
jupyter notebook 3_evaluate.ipynb
```

Reports accuracy by decoding strategy and by number count (harder puzzles have more numbers).

---

## 💡 Implementation Details

### Teacher inference prompt (Qwen3-8B)

```
<|im_start|>system
You are a helpful assistant that solves countdown math puzzles.<|im_end|>
<|im_start|>user
Given the target number 61 and the list of numbers [75, 80, 90, 24],
find a mathematical expression using basic operations (+, -, *, /) that equals the target.
You can use each number exactly once.
Provide your answer as a mathematical expression.
<|im_start|>assistant
```

Qwen3-8B is capable enough to reliably solve these puzzles. Its outputs (the expressions) are used directly as training labels for Gemma — no chain-of-thought, just the final answer.

### Student training format (Gemma chat template)

```
<start_of_turn>user
Given the target number 61 and the list of numbers [75, 80, 90, 24],
find a mathematical expression using basic operations (+, -, *, /) that equals the target.
You can use each number exactly once.
Provide your answer as a mathematical expression.<end_of_turn>
<start_of_turn>model
(90 - 75 + 80) / 24 ...<end_of_turn>
```

### Why Top-5 works

At inference, instead of greedy decoding, the model generates 5 candidate expressions with sampling (temperature=0.3, top_p=0.9). The puzzle is marked correct if any candidate evaluates to the target. This simple change nearly doubles accuracy from 43% → 86% with zero additional training.

---

## Created by Foutx
